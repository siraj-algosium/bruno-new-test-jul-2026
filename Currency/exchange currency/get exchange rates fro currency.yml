info:
  name: get exchange rates fro currency
  type: http
  seq: 2

http:
  method: GET
  url: "{{base}}/exchange-rates/latest?currency-id=14"
  headers:
    - name: branch
      value: "11"
  params:
    - name: currency-id
      value: "14"
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
