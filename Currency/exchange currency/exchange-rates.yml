info:
  name: exchange-rates
  type: http
  seq: 1

http:
  method: GET
  url: "{{base}}/exchange-rates"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
