info:
  name: get transaction currency
  type: http
  seq: 3

http:
  method: GET
  url: "{{base}}/transaction-currency"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
