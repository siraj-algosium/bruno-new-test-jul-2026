info:
  name: currency set up
  type: http
  seq: 1

http:
  method: GET
  url: "{{base}}/currency-setup"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
