info:
  name: update active status
  type: http
  seq: 4

http:
  method: PUT
  url: "{{base}}/global-currency/1/update-active-status"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: "{\r\n    \"is_active\": false\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
