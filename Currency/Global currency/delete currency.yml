info:
  name: delete currency
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base}}/global-currency/1"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: "{\r\n    \"is_active\": false\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
