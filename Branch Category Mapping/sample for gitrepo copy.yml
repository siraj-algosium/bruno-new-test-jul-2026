info:
  name: sample for gitrepo copy
  type: http
  seq: 2

http:
  method: GET
  url: https://www.google.com?test=two&test=one&three=three
  params:
    - name: test
      value: two
      type: query
    - name: test
      value: one
      type: query
    - name: three
      value: three
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
