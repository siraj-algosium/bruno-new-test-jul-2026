info:
  name: sample for gitrepo
  type: http
  seq: 3

http:
  method: GET
  url: "https://www.google.com?test=one "
  params:
    - name: test
      value: "one "
      type: query
      description: two
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
