info:
  name: sample for gitrepo
  type: http
  seq: 3

http:
  method: GET
  url: https://www.google.com?test=two&test=one
  params:
    - name: test
      value: two
      type: query
    - name: test
      value: one
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
