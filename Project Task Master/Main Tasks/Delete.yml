info:
  name: Delete
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base}}/project-master/main-tasks/3"
  headers:
    - name: Branch
      value: "1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
