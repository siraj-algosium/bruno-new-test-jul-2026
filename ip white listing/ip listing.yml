info:
  name: ip listing
  type: http
  seq: 1

http:
  method: GET
  url: "{{base}}/whitelisted-ips"
  headers:
    - name: branch
      value: "1"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
