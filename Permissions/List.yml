info:
  name: List
  type: http
  seq: 1

http:
  method: GET
  url: "{{base}}/permissions"
  headers:
    - name: Branch
      value: "11"
    - name: Accept
      value: application/json
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
