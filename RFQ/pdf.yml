info:
  name: pdf
  type: http
  seq: 6

http:
  method: GET
  url: "{{base}}/rfq/25/pdf"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
