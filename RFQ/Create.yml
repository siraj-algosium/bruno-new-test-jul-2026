info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{base}}/rfq"
  headers:
    - name: branch
      value: "11"
  params:
    - name: per_page
      value: "1"
      type: query
      disabled: true
    - name: page
      value: "2"
      type: query
      disabled: true
  body:
    type: json
    data: "{\r

      \    \"buyout_ids\": [61, 60]\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
