info:
  name: list
  type: http
  seq: 1

http:
  method: GET
  url: "{{base}}/purchase-request/1/attachments"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: |-
      {
          "is_approval_verified": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
