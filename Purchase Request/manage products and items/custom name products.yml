info:
  name: custom name products
  type: http
  seq: 22

http:
  method: PUT
  url: "{{base}}/purchase-request/56/products/387/update-label"
  headers:
    - name: Branch
      value: "11"
  body:
    type: json
    data: |-
      {
          "label": "Anode SACP test custom naming"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
