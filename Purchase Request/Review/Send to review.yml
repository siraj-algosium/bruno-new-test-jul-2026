info:
  name: Send to review
  type: http
  seq: 1

http:
  method: POST
  url: "{{base}}/projects/2/purchase-request/1/send-to-review"
  headers:
    - name: branch
      value: "11"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
