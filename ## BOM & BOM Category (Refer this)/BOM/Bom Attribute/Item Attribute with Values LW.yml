info:
  name: Item Attribute with Values LW
  type: http
  seq: 3

http:
  method: GET
  url: "{{base}}/items/1/attribute-with-values"
  headers:
    - name: Branch
      value: "1"
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
