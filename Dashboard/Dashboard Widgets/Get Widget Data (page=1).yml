info:
  name: Get Widget Data (page=1)
  type: http
  seq: 7

http:
  method: GET
  url: "{{base}}/dashboard/widgets/2/data?page=1"
  headers:
    - name: Accept
      value: application/json
    - name: branch
      value: "{{branch_id}}"
  params:
    - name: page
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
