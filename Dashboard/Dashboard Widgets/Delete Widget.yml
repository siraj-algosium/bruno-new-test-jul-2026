info:
  name: Delete Widget
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{base}}/dashboard/widgets/1"
  headers:
    - name: Accept
      value: application/json
    - name: branch
      value: "{{branch_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
