info:
  name: List Widgets
  type: http
  seq: 2

http:
  method: GET
  url: "{{base}}/dashboard/widgets"
  headers:
    - name: Accept
      value: application/json
    - name: branch
      value: "{{branch_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
