info:
  name: List Saved Filters by Module (contract_review) Copy
  type: http
  seq: 3

http:
  method: GET
  url: "{{base}}/dashboard/saved-filters/1"
  headers:
    - name: Accept
      value: application/json
    - name: branch
      value: "{{branch_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
