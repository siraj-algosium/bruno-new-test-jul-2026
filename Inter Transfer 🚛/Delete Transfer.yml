info:
  name: Delete Transfer
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{base}}/inventory-inter-transfers/16"
  headers:
    - name: Branch
      value: "1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
