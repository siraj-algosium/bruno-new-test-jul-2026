info:
  name: Complete Transfer
  type: http
  seq: 9

http:
  method: PUT
  url: "{{base}}/inventory-inter-transfers/4/complete"
  headers:
    - name: Branch
      value: "1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
