info:
  name: Complete Transfer
  type: http
  seq: 10

http:
  method: PUT
  url: "{{base}}/inventory-location-transfers/4/complete"
  headers:
    - name: Branch
      value: "1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
