info:
  name: contract-review
  type: http
  seq: 3

http:
  method: GET
  url: "{{base}}/temporary-url/contract-attachments/184"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
