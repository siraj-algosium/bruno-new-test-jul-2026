info:
  name: temporary url
  type: http
  seq: 1

http:
  method: GET
  url: "{{base}}/temporary-url/user/14"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
