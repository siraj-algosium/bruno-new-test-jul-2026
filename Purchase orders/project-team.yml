info:
  name: project-team
  type: http
  seq: 20

http:
  method: POST
  url: "{{base}}/purchase-order/28/update-status/project-team"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: "{\r

      \    \"is_reviewed\":1,\r

      \    \"comment\":\"asd\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
