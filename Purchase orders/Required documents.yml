info:
  name: Required documents
  type: http
  seq: 12

http:
  method: GET
  url: "{{base}}/purchase-order/3/required-documents"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
