info:
  name: account-team
  type: http
  seq: 21

http:
  method: POST
  url: "{{base}}/purchase-order/28/update-status/account-team"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: "{\r

      \    \"is_reviewed\":1,\r

      \    \"comment\":\"test 123\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
