info:
  name: comments
  type: http
  seq: 24

http:
  method: GET
  url: "{{base}}/purchase-order/4/comments"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
