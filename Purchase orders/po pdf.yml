info:
  name: po pdf
  type: http
  seq: 8

http:
  method: GET
  url: "{{base}}/purchase-order/4/pdf"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
