info:
  name: Save new Required documents
  type: http
  seq: 14

http:
  method: POST
  url: "{{base}}/required-documents"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: "{\r

      \    \"name\": \"Address proof\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
