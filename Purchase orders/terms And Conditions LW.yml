info:
  name: terms And Conditions LW
  type: http
  seq: 10

http:
  method: GET
  url: "{{base}}/purchase-order/3/terms-and-conditions"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
