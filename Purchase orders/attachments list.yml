info:
  name: attachments list
  type: http
  seq: 15

http:
  method: GET
  url: "{{base}}/purchase-order/3/attachments"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
