info:
  name: Move to In Transit
  type: http
  seq: 11

http:
  method: PUT
  url: "{{base}}/inventory-transfer-orders/1/move-to-in-transit"
  headers:
    - name: Branch
      value: "1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
