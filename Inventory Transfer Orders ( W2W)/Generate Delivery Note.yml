info:
  name: Generate Delivery Note
  type: http
  seq: 13

http:
  method: GET
  url: "{{base}}/inventory-transfer-orders/26/generate-delivery-note"
  headers:
    - name: Branch
      value: "11"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
