info:
  name: Delete Transfer
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{base}}/inventory-transfer-orders/4"
  headers:
    - name: Branch
      value: "11"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
