info:
  name: store
  type: http
  seq: 1

http:
  method: POST
  url: "{{base1}}/items/146/makes"
  headers:
    - name: Branch
      value: "1"
  body:
    type: json
    data: "{\r\n    \"make_ids\" : [2,3]\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
