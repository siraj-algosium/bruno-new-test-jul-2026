info:
  name: list
  type: http
  seq: 2

http:
  method: GET
  url: "{{base}}/makes"
  headers:
    - name: Branch
      value: "1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
