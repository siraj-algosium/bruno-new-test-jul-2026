info:
  name: list
  type: http
  seq: 1

http:
  method: GET
  url: "{{base}}/items/1/attribute?name=col"
  headers:
    - name: Branch
      value: "1"
  params:
    - name: getAll
      value: "true"
      type: query
      disabled: true
    - name: name
      value: col
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
