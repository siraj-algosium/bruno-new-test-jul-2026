info:
  name: Update Status
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{base}}/items/1/status"
  headers:
    - name: Branch
      value: "1"
  body:
    type: json
    data: "{\r\n    \"status\":true\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
