info:
  name: Show
  type: http
  seq: 5

http:
  method: GET
  url: "{{base}}/terms-and-conditions/4"
  headers:
    - name: branch
      value: "11"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
