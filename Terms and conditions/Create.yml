info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{base}}/terms-and-conditions"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: |-
      {
          "title" : "test 335",
          "description" : "Desccc",
          "content" : "<h1>w</h1>"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
