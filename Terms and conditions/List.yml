info:
  name: List
  type: http
  seq: 4

http:
  method: GET
  url: "{{base}}/terms-and-conditions"
  headers:
    - name: branch
      value: "11"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
