info:
  name: update
  type: http
  seq: 2

http:
  method: PUT
  url: "{{base}}/terms-and-conditions/4"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: |-
      {
          "title" : "test 336",
          "description" : "Desccc 123",
          "content" : "<h1>w</h1>"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
