info:
  name: Show
  type: http
  seq: 2

http:
  method: GET
  url: "{{base1}}/customer/contactPerson/4"
  headers:
    - name: branch
      value: "1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
