info:
  name: List
  type: http
  seq: 1

http:
  method: GET
  url: "{{base}}/customer/contactPerson"
  headers:
    - name: branch
      value: "11"
  params:
    - name: contractor
      value: "51"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
