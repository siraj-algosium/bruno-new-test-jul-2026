info:
  name: update active status
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{base}}/bom-templates/3/update-active-status"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: |-
      {
          "name":"Template 3",
          "description":"description template 3",
          "bom_category_id":4
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
