info:
  name: Update Bom Quantity
  type: http
  seq: 7

http:
  method: PUT
  url: "{{base}}/projects/25/bom/67/updateQuantity"
  headers:
    - name: Branch
      value: "11"
  body:
    type: json
    data: |-
      {
          "quantity":20
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
