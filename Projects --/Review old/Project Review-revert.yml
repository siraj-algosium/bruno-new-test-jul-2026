info:
  name: Project Review/revert
  type: http
  seq: 3

http:
  method: PUT
  url: "{{base}}/projects/3/update-review"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: |-
      {
          "is_reviewed": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
