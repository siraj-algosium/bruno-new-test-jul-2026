info:
  name: Reviewer item verify
  type: http
  seq: 2

http:
  method: POST
  url: "{{base}}/projects/3/items/3/reviewer-verify"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: |-
      {
          "is_review_verified": true
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
