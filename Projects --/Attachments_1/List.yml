info:
  name: List
  type: http
  seq: 1

http:
  method: GET
  url: "{{base}}/projects/2/purchase-request/2/consumable-allocation/attachments"
  headers:
    - name: Branch
      value: "11"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
