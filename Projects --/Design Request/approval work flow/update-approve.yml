info:
  name: update-approve
  type: http
  seq: 2

http:
  method: POST
  url: "{{base1}}/project_design_request/2/update-approve"
  headers:
    - name: branch
      value: "1"
  body:
    type: json
    data: "{\r\n    \"is_approved\":false\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
