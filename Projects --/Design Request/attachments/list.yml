info:
  name: list
  type: http
  seq: 2

http:
  method: GET
  url: "{{base}}/project_design_request/1/attachments"
  headers:
    - name: branch
      value: "11"
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
