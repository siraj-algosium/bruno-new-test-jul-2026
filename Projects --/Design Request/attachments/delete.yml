info:
  name: delete
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{base}}/project_design_request/1/attachments/1"
  headers:
    - name: branch
      value: "11"
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
