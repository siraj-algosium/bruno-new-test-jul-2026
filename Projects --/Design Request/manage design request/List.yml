info:
  name: List
  type: http
  seq: 2

http:
  method: GET
  url: "{{base}}/project_design_request"
  headers:
    - name: branch
      value: "11"
  params:
    - name: per_page
      value: "1"
      type: query
      disabled: true
    - name: page
      value: "30"
      type: query
      disabled: true
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
