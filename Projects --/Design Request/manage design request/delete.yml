info:
  name: delete
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base}}/project_design_request/2"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
