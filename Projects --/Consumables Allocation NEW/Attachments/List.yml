info:
  name: List
  type: http
  seq: 1

http:
  method: GET
  url: "{{base}}/purchase-request/3/consumable-allocation/attachments"
  headers:
    - name: Branch
      value: "1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
