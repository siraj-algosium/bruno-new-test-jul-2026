info:
  name: delete project
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{base}}/projects/1"
  headers:
    - name: branch
      value: "11"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
