info:
  name: subtask comments edit
  type: http
  seq: 3

http:
  method: PUT
  url: "{{base1}}/projects/25/project-planning/sub-tasks/38/comments/11"
  headers:
    - name: branch
      value: "1"
  body:
    type: json
    data: "{\r

      \    \"comment\": \"sub task list\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
