info:
  name: update-approve
  type: http
  seq: 2

http:
  method: POST
  url: "{{base}}/projects/31/project-planning/update-approve"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: "{\r\n    \"is_approved\" : true\r\n}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
