info:
  name: Add PO items to Draft
  type: http
  seq: 4

http:
  method: POST
  url: "{{base}}/purchase-entry/1/items"
  headers:
    - name: Branch
      value: "11"
  body:
    type: json
    data: "{\r

      \   \"purchase_order_item_ids\": [9]\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
