info:
  name: Show
  type: http
  seq: 4

http:
  method: GET
  url: "{{base}}/customer/160"
  headers:
    - name: branch
      value: "11"
  params:
    - name: branch
      value: "11"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
