info:
  name: Remove One Mapped Category
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{base}}/business-category-groups/1/categories/4"
  headers:
    - name: branch
      value: "{{branch_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
