info:
  name: Update (Add-only categories)
  type: http
  seq: 6

http:
  method: PUT
  url: "{{base}}/business-category-groups/1"
  headers:
    - name: branch
      value: "{{branch_id}}"
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Solar Group",
        "description": "Updated description"
        //"categories": [1, 2 , 4]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
