info:
  name: logout
  type: http
  seq: 2

http:
  method: POST
  url: "{{base}}/logout"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
