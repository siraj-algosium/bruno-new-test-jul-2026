info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{base}}/login"
  body:
    type: json
    data: |-
      {
        "username": "admin",
        "password": "admin@2024"
      }

      // {
      //   "username": "venice",
      //   "password": "123456"
      // }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
