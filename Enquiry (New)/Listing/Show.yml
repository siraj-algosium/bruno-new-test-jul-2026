info:
  name: Show
  type: http
  seq: 3

http:
  method: GET
  url: "{{base}}/enquiries/667"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{dev-darshana}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
