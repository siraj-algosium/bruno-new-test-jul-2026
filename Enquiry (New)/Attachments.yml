info:
  name: Attachments
  type: http
  seq: 20

http:
  method: GET
  url: "{{base}}/enquiries/88/attachment?page=1&per_page=2"
  headers:
    - name: branch
      value: "1"
  params:
    - name: page
      value: "1"
      type: query
    - name: per_page
      value: "2"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
