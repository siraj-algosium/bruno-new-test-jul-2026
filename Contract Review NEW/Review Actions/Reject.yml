info:
  name: Reject
  type: http
  seq: 3

http:
  method: POST
  url: "{{base}}/contract-reviews/3752/reject"
  headers:
    - name: Branch
      value: "1"
  body:
    type: json
    data: |-
      {
         "remarks": " rejecting rev 0 2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
