info:
  name: Approve
  type: http
  seq: 2

http:
  method: POST
  url: "{{base}}/contract-reviews/3752/approve"
  headers:
    - name: Branch
      value: "1"
  body:
    type: json
    data: |-
      {
         "remarks": " approving"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
