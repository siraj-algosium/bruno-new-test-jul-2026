info:
  name: History
  type: http
  seq: 3

http:
  method: GET
  url: "{{base}}/contract-reviews/158/revisions/1/history"
  headers:
    - name: branch
      value: "11"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
