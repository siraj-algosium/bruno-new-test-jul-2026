info:
  name: list
  type: http
  seq: 1

http:
  method: GET
  url: "{{base}}/items/8/attribute/1/value"
  headers:
    - name: Branch
      value: "1"
  params:
    - name: getAll
      value: "true"
      type: query
      disabled: true
    - name: name
      value: "1"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
