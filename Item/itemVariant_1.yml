info:
  name: itemVariant_1
  type: http
  seq: 7

http:
  method: GET
  url: "{{base}}/itemVariant/86"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
