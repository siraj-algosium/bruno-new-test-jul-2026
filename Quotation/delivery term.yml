info:
  name: delivery term
  type: http
  seq: 5

http:
  method: GET
  url: "{{base}}/delivery-term-lw"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
