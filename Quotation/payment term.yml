info:
  name: payment term
  type: http
  seq: 4

http:
  method: GET
  url: "{{base}}/payment-term-lw"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
