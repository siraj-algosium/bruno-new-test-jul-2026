info:
  name: comments_1
  type: http
  seq: 15

http:
  method: GET
  url: "{{base}}/rfq/9/quotation/comments"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
