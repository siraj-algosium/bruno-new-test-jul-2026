info:
  name: vendor revisions for comparision
  type: http
  seq: 8

http:
  method: GET
  url: "{{base}}/rfq/9/quotation/compare/vendor/44"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
