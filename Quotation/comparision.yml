info:
  name: comparision
  type: http
  seq: 7

http:
  method: GET
  url: "{{base}}/rfq/9/quotation/compare?currency-type=local"
  headers:
    - name: branch
      value: "11"
  params:
    - name: currency-type
      value: local
      type: query
      description: group, transaction
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
