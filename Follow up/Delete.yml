info:
  name: Delete
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{base}}/followUp/39"
  headers:
    - name: branch
      value: "11"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
