info:
  name: Show
  type: http
  seq: 3

http:
  method: GET
  url: "{{base}}/client/contactPerson/15"
  headers:
    - name: branch
      value: "11"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
