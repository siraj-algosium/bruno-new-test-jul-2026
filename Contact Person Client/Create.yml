info:
  name: Create
  type: http
  seq: 2

http:
  method: POST
  url: "{{base}}/client/contactPerson"
  headers:
    - name: branch
      value: "11"
  body:
    type: json
    data: |-
      {
        "name":"testname",
        "contactNumber":"12345678",
        "client_id":7
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
