info:
  name: Delete
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base}}/client/contactPerson/15"
  headers:
    - name: branch
      value: "11"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
