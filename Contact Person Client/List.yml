info:
  name: List
  type: http
  seq: 1

http:
  method: GET
  url: "{{base}}/client/contactPerson?client=6"
  headers:
    - name: branch
      value: "11"
  params:
    - name: client
      value: "6"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
